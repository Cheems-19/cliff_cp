# 预注册分析计划（Pre-registration / Analysis Plan）

> 目的：在正式投稿前**固定**假设、统计口径与判定规则，避免"事后挑选显著结果"（p-hacking / HARKing）。
> 论文正文与附录的所有主张按本文档分层与判定，凡超出本计划的分析一律标注为 exploratory。
> 版本：v1 · 2026-09-23。冻结后如需修改，须在附录记录修改理由与时间。

---

## 0. 固定设置（不参与搜索）

| 项 | 值 |
|---|---|
| 数据集 | MoleculeACE 中满足 N ≥ 800 且数据集级悬崖分子 ≥ 60 的 15 个 ChEMBL 靶点集 |
| 表示 | 预测器特征 ECFP4-2048 或 RDKit 2D 描述符（约 210 维）；**悬崖判定与邻域检索恒用 ECFP4** |
| 划分 | 随机划分（主）；scaffold 划分（Bemis–Murcko，附加分析） |
| 分割比 | train/cal/test = 60/20/20 |
| 名义覆盖率 | α = 0.10（主）；α ∈ {0.07,0.10,0.13,0.16}（覆盖率匹配用） |
| 悬崖定义 | `cliffA`（数据集级：∃ sim ≥ 0.85 且 \|ΔpKi\| ≥ 1.0）；`cliffB`（机制：训练邻居 sim ≥ 0.85 且 \|Δy\| ≥ 1.0 的高活性一侧） |
| 模型族 | RF（200 树）、GBR（5 成员多种子集成）、D-MPNN（chemprop 2.3.1，3 成员） |
| 种子 | 主分析 10 个；跨架构/敏感性 4–5 个（逐一注明） |
| 方向一致性 | 报告"方向正确数据集数 / 总数"；**低于 15/15 的结论必须带范围** |

**统计口径（固定）**：跨数据集合并先在 (数据集, 种子) 内配对，再在数据集内跨种子求均值/标准误，最后随机效应合并（报 Δ、p、I²）；覆盖率区间用 Wilson；覆盖率回测用**划分层面 z 检验**（不是单划分内的二项检验）。

---

## 1. 主要（confirmatory）假设

> 判定规则：随机效应合并 p < 0.05 **且** 方向一致性 ≥ 13/15；多重比较用 Holm–Bonferroni 控制在 primary 族内（4 个检验）。

| ID | 假设 | 统计量 | 判定 |
|---|---|---|---|
| **P1** | 悬崖组边缘覆盖率低于非悬崖组 | Δ覆盖（非悬崖 − 悬崖），配对 Wilcoxon | p<0.05 且 ≥13/15 |
| **P2** | 域内悬崖分子的失败率高于域内非悬崖分子 | 失败率比（2 族 AD × 4 阈值 = 8 界定） | 8/8 方向一致且比值 > 1.5 |
| **P3** | 按**错配信号**（`d_nb`、`rf_std`）条件化会使悬崖组覆盖率低于 global | Δ覆盖配对 Wilcoxon，覆盖率匹配口径 | p<0.05 且方向 ≤4/15 反向 |
| **P4** | 按**对齐信号**（`d_wstd`）条件化不伤害悬崖组（方向一致为正） | Δ覆盖配对 Wilcoxon + 方向一致性 | 方向一致性 ≥ 10/15（不要求 p<0.05） |

> P4 的弱判定是**事先声明**的：该效应量小且功效有限（见 §4 局限），故"不伤害"以方向一致性判定，不以显著性判定。

---

## 2. 次要（secondary）假设

| ID | 假设 | 判据 |
|---|---|---|
| S1 | 表示轴复现（ECFP4 → RDKit 2D，8 个分区变量符号一致） | 8/8 符号一致 |
| S2 | 模型族轴复现（RF → GBR；`d_nb` 有害、`d_wstd` 安全） | 两族方向一致 |
| S3 | D-MPNN 上 `rf_std` 无害（其集成方差错配量小） | 方向与错配量排序一致 |
| S4 | 覆盖率回测：所有方法边缘覆盖在名义值 ±0.3 pp 内 | 划分层面 z 检验 |
| S5 | 方法阶梯排序与错配量预言一致（CQR < global < 错配信号） | 排序一致 |
| S6 | **scaffold 划分下 P1 仍成立**（划分现实性） | 随机效应合并 p<0.05 |
| S7 | **adaptive / weighted CP 不消除悬崖差距**（基线完整性） | 二者 Δ悬崖覆盖 vs global 不显著为正 |
| S8 | **决策影响**：真活性悬崖分子被低估的比例高于非悬崖 | 逐数据集配对，方向一致性 ≥13/15 |

> S6–S8 是 v1 新增（对应"审稿人可攻击的方法学面"）。S7 的**预期方向**：adaptive/weighted 改善边缘覆盖或宽度，但**不专门修复悬崖组**；若实测相反，如实报告并修改论文结论。

---

## 3. 探索性（exploratory，不用于主要结论）

- 错配量与伤害的定量关系（含 `r = −0.63`）、`d_nn_sim` 反例
- `d_wstd` 覆盖率匹配下的 +1.2 pp（p≈0.099）
- GNN 集成成员 3 → 8 的效应（已知混杂，仅作观察）
- `n_bins` / `sim` / `act` 阈值扫描的细节
- **待补的 clean 机制检验**：固定预测器、只变信号的精度阶梯（RF 树子集方差）
- 决策影响的 τ 扫描与预算曲线
- **覆盖转移恒等式（命题 1）与无标签诊断量 `net_mag`（式 5）**：恒等式为精确式（无分布假设），
  其经验验证（ρ=0.865，n=16）基于已完成的运行、属**事后验证**，故在论文中作"理论 + 验证"呈现，
  不参与 primary 显著性宣称

---

## 4. 反例与失效处理的义务（必须报告）

1. `d_nn_sim`：错配量最大却不伤害 → 证明错配量是**甄别规则非定量公式**。
2. `d_iqr`：错配量≈0 却无效 → "错配量≈0"是**必要非充分**条件。
3. `act=1.5` 下 `rf_std` 结论反转（仅 9 数据集）→ 限定 `rf_std` 有害性于 `act ∈ {0.5,1.0}`。
4. `sim=0.95` 配置不可用（15 集仅 1 存活）→ 记为任务稀疏性。
5. GNN 仅 3 个集成成员、I²=79% → 记为局限。

---

## 5. 多重比较与报告规范

- **primary 族**（P1–P4）用 Holm–Bonferroni；报告校正后 q 值。
- **secondary/exploratory** 只报效应量 + 95% CI + 方向一致性，**不宣称显著性**。
- 所有覆盖率差距一律报 Wilson 区间；所有合并量报 I²。
- 每个数字必须可由脚本一键重生成（脚本路径写进附录）。
- 阈值的"最佳值"不得作为结论报告（`n_bins` 只给建议区间 3 或 5）。

---

## 6. S6–S8 的结论（2026-09-23 回填）

| ID | 判定规则 | 实测 | 结论 |
|---|---|---|---|
| **S6** | 骨架划分下 P1 仍成立：随机效应合并 p<0.05 | 骨架 · cliffA：Δ=+5.0 pp，方向一致 13/15，Wilcoxon p=4.3×10⁻⁴（随机 · cliffA：+9.5 pp，14/15；随机 · cliffB：+17.3 pp，15/15；骨架 · cliffB：+8.9 pp，12/15） | ✅ **成立**，但**幅度减半**（9.5→5.0 pp），须如实报告为"部分依赖划分" |
| **S7** | adaptive / weighted 不消除悬崖差距（二者 Δ vs global 不显著为正） | 最优基线 CQR（随机 +2.73 pp）之后残余 6.8 pp；**adaptive 随机 +2.54 pp、p=0.055，代价是宽度 +21%**；weighted 随机 −1.26 pp / 骨架 −1.94 pp | ✅ **成立**（差距未被消除），但**预期方向部分被证伪**，见下 |
| **S8** | 决策影响：真活性悬崖分子被低估比例高于非悬崖，方向一致 ≥13/15 | 悬崖 35.3% vs 非悬崖 13.3%（+22.0 pp，2.66×），15/15 | ✅ 成立（已在 §3.7） |

### ⚠️ 预期被部分证伪（必须如实报告）
v1 对 S7 的预期是"adaptive / weighted **改善边缘覆盖或宽度，但不专门修复悬崖组**；若实测相反，如实报告并修改论文结论"。
实测：**adaptive 确实专门改善了悬崖组**（随机划分下 +2.54 pp，cliffB 口径 +6.37 pp、13/15、p=3.4×10⁻³），
这与"不专门修复悬崖组"的预期**相反**。处理方式：
1. 论文 §3.8 明确写出这一点，并说明**补上这条基线是必要的**——否则"除 CQR 外都无法改善"会是过强表述；
2. 同时报告其**代价**（宽度 +21% / +28%）与**骨架划分下增益消失**（−0.34 pp），故不改变主结论；
3. 本条记入本表，不修改原判读标准（标准保持数据前写死的状态）。

### 实现缺陷披露（影响表 4 两行）
phase11 首跑时 **Mondrian ← `d_wstd` / `d_nb` 两臂静默退化为 global**（Δ 恰为 0.00 pp、方向一致 0/15、Wilcoxon p=nan）。
根因：`dispersion.neighbor_label_stats` 对**无训练邻居分子**把 `d_wstd` 等留为 NaN（"无邻居"分支只设了 `d_nb` 与 `d_pair_gap`），
而 `bins_by_cal` 直接把含 NaN 的数组交给 `np.quantile`，边界全部退化为 NaN，`searchsorted` 使所有分子落入同一箱；
该箱样本数必然达标，于是回退为全局分位数。
修复：按主流水线既定约定把 NaN 归入**最稀疏箱**（NaN→0），并补 `mondrian_nb` 一臂后重跑；表 4 两行以补跑数字为准。

**补跑结果（2026-09-23 17:12 完成，rows=600 = 15×2×5×4 ✓）**：
Mondrian ← `d_wstd`（对齐）Δ = **+0.36 pp**（随机，9/15，p=0.36）/ **−0.71 pp**（骨架，5/15，p=0.19）；
Mondrian ← `d_nb`（错配）Δ = **−2.70 pp**（随机，3/15，p=6.3×10⁻³）/ **−3.82 pp**（骨架，2/15，p=1.5×10⁻³）。
→ 修复后**"对齐不伤害 / 错配显著有害"的对比在两种划分下都成立**，与 §3.2 的错配量预言一致。
**可复现性交叉校验**：补跑与首跑在同一 (划分, 数据集, 种子) 上的 `mondrian_rfstd` 覆盖率**逐点完全一致
（150 组，最大绝对差 0.000）**，说明两跑确定性地可复现，两行数字可拼接进同一张表。
**性质**：实现修复，**不改变任何判读标准与其它臂的数字**。
| 2026-09-23 | 摘要/§3.1 的幅度结论改为**并列报两种划分协议**（随机 +18.2 pp / 骨架 +8.9 pp），并新增限制 15「幅度依赖划分协议」 | 回应「头条数字取自最不现实的随机划分」这一可预期的审稿质疑；不改动任何分析或数字 |
| 2026-09-23 | 新增 §3.9 偏移强度谱（phase15：5 协议 × 15 数据集 × 3 种子）——**探索性，非预注册**；活性偏移为构造的极端外推 | 回应「可交换性被打破的证据在哪」；判读标准在 §3.9 首次写明（先于解读、后于设计） |

---

## 7. 本计划之外的改动记录

| 日期 | 改动 | 理由 |
|---|---|---|
| 2026-09-23 | 新增 S6–S8（scaffold / adaptive-weighted / 决策影响） | 补"划分现实性、基线完整性、决策影响"三处审稿人攻击面 |
| 2026-09-23 | 摘要覆盖率差距口径由"20–24 pp"改为 **18.2 pp**（逐数据集 7.7–32.4，15/15） | 原值无数据支撑；实测 macro α=0.10 为 18.2 pp |
| 2026-09-24 | C2 偏移强度谱补插值对照 act_mid20（测试集=效力中位带）；分类外推 §3.10 升级为 5 数据集 × 10 种子 + APS 基线（均标注探索性/事后扩展） | 应审稿潜在质疑补全谱线与外部效度；判读标准先于解读、后于设计 |
| 2026-09-23 | 新增理论小节 §2.3.1（覆盖转移恒等式 + `net_mag`），列入 §3 exploratory | 事后推导与验证，不进入 primary 宣称 |
| 2026-09-23 | phase11/13 完成并回填 §3.8（表 4/表 5）；S6/S7/S8 判定见 §6 | 预注册的三个次要假设全部有结论 |
| 2026-09-23 | 修复 phase11 的 Mondrian←`d_wstd` / `d_nb` 实现缺陷（NaN 分箱塌陷→静默退化为 global），补跑后回填 | 首跑该臂 Δ≡0.00 pp、p=nan，核查为**伪结果而非零效应**；属实现修复，不改判读标准 |
| 2026-09-23 | 记录 S7 预期方向被部分证伪（adaptive 确实改善悬崖组） | 数据前写死的预期与实测相反时，如实报告并保留原标准 |
